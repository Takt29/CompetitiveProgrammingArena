{
  "name": "competitive-programming-arena",
  "version": "0.1.0",
  "description": "website for competitive programming virtual contest",
  "scripts": {
    "dev": "firebase emulators:start --import=./firebase-data --export-on-exit --project x-local-emu",
    "login": "firebase login",
    "deploy": "firebase deploy",
    "deploy:functions": "npm run deploy -- --only functions",
    "deploy:hosting": "npm run deploy -- --only hosting",
    "deploy:firestore": "npm run deploy -- --only firestore",
    "deploy:storage": "npm run deploy -- --only storage",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Takt29/CompetitiveProgrammingArena.git"
  },
  "author": "Takt29",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Takt29/CompetitiveProgrammingArena/issues"
  },
  "homepage": "https://github.com/Takt29/CompetitiveProgrammingArena#readme",
  "devDependencies": {
    "firebase-admin": "^10.0.1",
    "firebase-tools": "^10.0.1"
  }
}
